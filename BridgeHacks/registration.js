 
/*function signUp() {
    var user = document.getElementById("username").value;
    var password = document.getElementById("password").value;
    var confirmPassword = document.getElementById("confirmPassword").value;
    var email = document.getElementById("email").value;
    var fName = document.getElementById("fName").value;
    var lName = document.getElementById("lName").value;
    var age = document.getElementById("age").value;

    if (password != confirmPassword)
        alert("Your password and confirm password are different, please enter again :)");
    else{
        

        const fs = require('fs');
        // Data which will write in a file. 
        let data = "Learning how to write in a file.";
        
        // Write data in 'Output.txt' . 
        fs.appendFile('accounts.txt', data, (err) => { 
            
            // In case of a error throw err. 
            if (err) throw err; 
        }) 
            */
        
        
        /*
        alert("I made it this far motherfuckers wli has a cow");
        const fsLibrary = require ('fs');
        
        let data = user;
        fsLibrary.writeFile('accounts.txt', data, (error) => {
            if (error) throw err;
        })

        
// Requiring fs module in which 
// writeFile function is defined. 
const fs = require('fs'); 
  
// Data which will write in a file. 
let data = "Learniasdfasdfasdfasdfasdfng how to write in a file.";
  
// Write data in 'Output.txt' . 
fs.appendFile('Output.txt', data, (err) => { 
      
    // In case of a error throw err. 
    if (err) throw err; 
}) 

        */








        // const fs = require ('fs');
        // fs.writeFile('accounts.txt', user, (err) => {
        //     if (err) throw err;
        // })

        // const fs = require ('fs');
        // fs.writeFile('accounts.txt', password, (err) => {
        //     if (err) throw err;
        // })

        // const fs = require ('fs');
        // fs.writeFile('accounts.txt', email, (err) => {
        //     if (err) throw err;
        // })

        // const fs = require ('fs');
        // fs.writeFile('accounts.txt', fName, (err) => {
        //     if (err) throw err;
        // })

        // const fs = require ('fs');
        // fs.writeFile('accounts.txt', lName, (err) => {
        //     if (err) throw err;
        // })

        // const fs = require ('fs');
        // fs.writeFile('accounts.txt', age, (err) => {
        //     if (err) throw err;
        // })   
        
        /*
        const fs = require('fs');

        let person = {
            "username": user,
            "password": password,
            "email":email,
            "fName": fName,
            "lName":lName,
            "age":age
        }

        alert("I made it this far motherfuckers");
        let jsonText = JSON.stringify(obj);
        localStorage.setItem('user_info',jsonText)
        fs.writeFile("accounts.txt",jsonText, function (err) {
            if (err) throw err;
          });
        */



       // let data = "david sucking on a cow's cock"
       // fs.writeFile('accounts.txt', data, (err) => {

          //  if (err) throw err;
        //})
 //}
//}


/*
var name = window.prompt("Enter your name: ");
alert("Your name is " + name);

account in open("accounts.txt", "r").readlines(){
*/

/*
Create an arraylist for each element (e.g. username, password) and add() as each entry is recognized, try-catch,
then read all contents from file, then close the file

       for (a : ){
            loginaccount = account.split(",")
            if (user == loginaccount[0] && password == loginaccount[1]) {
                print("You are logged in.")
                return True
            }
            else{
                print("Incorrect. Please try again.")
                return False
            }
        }
    }
*/

/* Searching [index] for repeated usernames
*/

/*
username
password
email
fName
lName
age

repeat
*/